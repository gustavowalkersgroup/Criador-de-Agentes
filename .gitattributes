# Normaliza fim de linha: LF no repositório, evita ruído de CRLF e os erros
# "unable to unlink" em working tree de drive de rede (Windows/Z:).
* text=auto eol=lf

# Binários — nunca normalizar.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
*.zip binary
